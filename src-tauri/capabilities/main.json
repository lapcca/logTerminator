{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Permissions that are used by the main window of the application",
  "windows": ["main"],
  "permissions": [
    "dialog:default"
  ]
}
